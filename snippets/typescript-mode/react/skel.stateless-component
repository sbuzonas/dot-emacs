# -*- mode: snippet -*-
# contributor: Steve Buzonas
# name: React: Stateless Component
# key: _rsc
# binding: direct-keybinding
# expand-env: ((yas-indent-line 'auto) (yas-also-auto-indent-first-line t) (yas-wrap-around-region t))
# --
import * as React from 'react';

const ${1:`(file-name-nondirectory (file-name-sans-extension (buffer-file-name)))`}: React.SFC = () => (
    ${2:<div></div>}
);

export default $1;
